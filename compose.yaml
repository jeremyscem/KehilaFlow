services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: kehilaflow
      POSTGRES_USER: kehilaflow
      POSTGRES_PASSWORD: kehilaflow
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
